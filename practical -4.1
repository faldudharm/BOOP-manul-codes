#include<iostream>
using namespace std;

int sort(int &a, int &b)
{
    float c;
    if(a<=b)
    {
    }
    else{
    a=c;
    a=b;
    b=c;
    }
    return a;
    return b;
    }
    int main ()
    {
    int x,y;
        cout << "Enter two no." << endl;
        cin >> x >> y;
        sort(x,y);
        
        cout << "After sorting" << endl;
    cout << "x=" << x << endl;
    cout << "y=" << y << endl;
    return 0;
}